# Normalize to LF in the repository. Development happens on Windows and Vercel
# builds on Linux; without this, checkouts round-trip through CRLF and every
# file shows as fully rewritten in the next diff.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.ico binary
*.woff  binary
*.woff2 binary
